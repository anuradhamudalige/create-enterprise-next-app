'use client';

import React from 'react';

const Loading: React.FC = () => {
  return (
    <div className="w-full h-full min-h-dvh flex justify-center items-center">
      Some loading configuration must be place here
    </div>
  );
};

export default Loading;
